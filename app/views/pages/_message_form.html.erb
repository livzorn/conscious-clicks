<%= simple_form_for [message_set, message], remote: true do |f| %>
  <%= f.input :content, label: "message" %>
  <%= f.input :link, label: "link" %>
  <%= f.submit "add message", class: "btn-purple" %>
<% end %>
